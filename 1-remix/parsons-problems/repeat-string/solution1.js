/**
 * Repeat a string repetitions times
 *
 * @param {string} str - The string to repeat.
 * @param {number} repetitions - Number of times to repeat a string.
 * @returns The repeated str repetitions times.
 */

export const repeatString = (str = '', repetitions = 1) => {
    return str.repeat(repetitions);
};
